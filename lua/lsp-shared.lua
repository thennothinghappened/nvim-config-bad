-- Shared across LSP plugins
return {
    -- Configuration of the LSP servers we need
    servers = {
        lua_ls = {},
        rust_analyzer = {},
        clangd = {},
        cssls = {},
        html = {},
        tsserver = {}
    }
}
